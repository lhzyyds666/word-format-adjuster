# Word 格式调整器 TODO

## 本轮处理

- [x] 建立审核意见 TODO 清单。
- [x] 表格格式提取更贴近模板真实 XML：边距、边框色、表头加粗、重复表头、左/居中对齐。
- [x] 表格格式套用支持关闭重复表头，避免旧目标文档残留 `w:tblHeader`。
- [x] 无正文标题的文档不再把所有表格都当封面/前置表格跳过。
- [x] 题注标签动态提取更稳健：支持 `图表`、`附图`、`公式` 等标签，同时避免把普通正文误判为题注。
- [x] 题注套用只命中“标签 + 编号”的段落，避免 `图书馆...` 这类正文被误格式化。
- [x] 交叉引用占位符跨多个 `w:r` 时稳定替换，并保留占位符前后文字。
- [x] 为以上行为补充回归测试并运行 `npm test`。

## 后续高优队列

- [x] `applyTemplateProfile` 应尊重套用范围：未勾选正文/标题时不应直接复制 `styles.xml`，未勾选相关范围时不应直接覆盖 `settings.xml`。
- [x] `scanReferenceTargets` 应通过 `styles.xml` 的样式名或 `outlineLvl` 识别数字/自定义标题样式，而不是只看 styleId 文本。
- [x] 提取布尔型 OOXML 属性时尊重 `w:val="0"`、`false`、`off`。
- [x] 页眉页脚完整模板模式若复制图片/形状，需要同步 `.rels` 和 `word/media/*`；否则应安全剥离外部关系。
- [x] 预设 JSON 导入需要更严格的结构校验，避免半截 Profile 进入套用流程。
- [x] `first()` 当前返回后代首个元素，后续应区分直接子节点查询，减少嵌套属性误读。
- [x] UI 操作状态需要更细：导入模板或修改选项后，应清空旧输出/旧报告，忙碌状态禁用所有会修改文档的按钮。
